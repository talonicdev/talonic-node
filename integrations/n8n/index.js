// n8n discovers nodes and credentials via the `n8n` field in package.json
// (pointing at the compiled files in dist/). This entry exists only to
// satisfy `main`; it intentionally exports nothing.
module.exports = {}
